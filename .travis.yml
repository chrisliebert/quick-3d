language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake libsqlite3-dev swig2.0 libluajit-5.1-dev luajit
after_install:
  - cd examples/Lua
  - cargo build --verbose
